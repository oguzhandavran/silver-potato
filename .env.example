# Copy to .env (do not commit) and fill in your keys.
#
# The app also supports storing keys securely at runtime via SecureAiSettingsStore.

GEMINI_API_KEY=
OPENAI_API_KEY=
CLAUDE_API_KEY=

# Model toggles (optional, example format)
# AI_MODEL_TOGGLE_gemini:gemini-1.5-flash=true
